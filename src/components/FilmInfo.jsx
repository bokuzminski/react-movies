import React from "react";
import "../App.css";

const Movie = ({location, movie}) => {
  
  
  
  return (
    <div>
      <section>
        <h1>{modalTitle}</h1>
      </section>
      <p>{modaloverview}</p>
    </div>
  );
};

export default Movie;
